import PageShell from '../../components/common/PageShell.jsx';

const BudgetPage = () => (
  <PageShell title="Budget" subtitle="Plan and track financial budgets">
    <p className="welcome-text">Budget planning — connect to /api/v1/budgets.</p>
  </PageShell>
);

export default BudgetPage;
