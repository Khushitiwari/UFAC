# Docker Postgres (see docker-compose.yml — host port 5437)
POSTGRES_USER=khushi
POSTGRES_PASSWORD=soha
POSTGRES_DB=ufac

# Backend connection string (copy to backend/.env)
DATABASE_URL=postgresql://khushi:soha@localhost:5437/ufac?schema=public
