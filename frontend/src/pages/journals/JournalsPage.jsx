import PageShell from '../../components/common/PageShell.jsx';

const JournalsPage = () => (
  <PageShell title="Journals" subtitle="Record and manage journal entries">
    <p className="welcome-text">Journal types and entries — connect to /api/v1/journals.</p>
  </PageShell>
);

export default JournalsPage;
