import PageShell from '../../components/common/PageShell.jsx';

const JournalsPage = () => (
  <PageShell title="Journals">
    <p style={{ color: 'var(--color-muted)' }}>Journal types and entries — connect to /api/v1/journals.</p>
  </PageShell>
);

export default JournalsPage;
