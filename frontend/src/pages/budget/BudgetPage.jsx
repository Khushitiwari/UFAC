import PageShell from '../../components/common/PageShell.jsx';

const BudgetPage = () => (
  <PageShell title="Budget">
    <p style={{ color: 'var(--color-muted)' }}>Budget planning — connect to /api/v1/budgets.</p>
  </PageShell>
);

export default BudgetPage;
