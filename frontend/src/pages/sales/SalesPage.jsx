import PageShell from '../../components/common/PageShell.jsx';

const SalesPage = () => (
  <PageShell title="Sales">
    <p style={{ color: 'var(--color-muted)' }}>Sales orders and customer invoices — connect to /api/v1/sales.</p>
  </PageShell>
);

export default SalesPage;
