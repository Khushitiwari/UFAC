import PageShell from '../../components/common/PageShell.jsx';

const SalesPage = () => (
  <PageShell title="Sales" subtitle="Manage sales orders and customer invoices">
    <p className="welcome-text">Sales orders and customer invoices — connect to /api/v1/sales.</p>
  </PageShell>
);

export default SalesPage;
