import PageShell from '../../components/common/PageShell.jsx';

const ProductsPage = () => (
  <PageShell title="Products">
    <p style={{ color: 'var(--color-muted)' }}>Product catalog management — connect to /api/v1/products.</p>
  </PageShell>
);

export default ProductsPage;
