# Docker Postgres (see docker-compose.yml — host port 5437)
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=ufac

# Backend connection string (copy to backend/.env — replace placeholders)
DATABASE_URL=postgresql://your_postgres_user:your_postgres_password@localhost:5437/ufac?schema=public
