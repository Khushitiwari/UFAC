# Dev: relative URL — Vite proxies /api to http://localhost:5000 (no CORS issues)
VITE_API_BASE_URL=/api/v1

# Direct API (e.g. production or when not using Vite dev server):
# VITE_API_BASE_URL=http://localhost:5000/api/v1
