import PageShell from '../../components/common/PageShell.jsx';

const ProductsPage = () => (
  <PageShell title="Products" subtitle="Manage your product catalog and inventory items">
    <p className="welcome-text">Product catalog management — connect to /api/v1/products.</p>
  </PageShell>
);

export default ProductsPage;
