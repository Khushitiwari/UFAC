import PageShell from '../../components/common/PageShell.jsx';

const ChartOfAccountsPage = () => (
  <PageShell title="Chart of Accounts" subtitle="Define your account hierarchy and structure">
    <p className="welcome-text">Manage your account hierarchy — connect to /api/v1/accounts.</p>
  </PageShell>
);

export default ChartOfAccountsPage;
